/**
 * I{{aggregateClass}}Repository — Domain Repository Interface
 * Generated by ddd-codegen | Context: {{contextName}}
 * Metamodel: DCSL DAssoc + Repository pattern (Evans DDD)
 * Sits at Domain layer — infrastructure implements this interface.
 */

import { {{aggregateClass}} } from '../entities/{{aggregateClassKebab}}.entity';

export interface I{{aggregateClass}}Repository {
{{#each methods}}
  {{name}}({{#each parameters}}{{name}}: {{type}}{{#unless @last}}, {{/unless}}{{/each}}): {{returnType}};
{{/each}}
}

export const {{aggregateClass | uppercase}}_REPOSITORY_TOKEN = Symbol('I{{aggregateClass}}Repository');
