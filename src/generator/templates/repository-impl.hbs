/**
 * {{aggregateClass}}Repository — Infrastructure Repository Implementation
 * Generated by ddd-codegen | Context: {{contextName}}
 * Implements: I{{aggregateClass}}Repository
 * Layer: Infrastructure — adapts persistence to domain repository interface.
 *
 * Replace TypeORM with your preferred ORM / data-access layer.
 */

import { Injectable } from '@nestjs/common';
import { InjectRepository } from '@nestjs/typeorm';
import { Repository } from 'typeorm';
import { {{aggregateClass}} } from '../../domain/entities/{{aggregateClassKebab}}.entity';
import {
  I{{aggregateClass}}Repository,
  {{aggregateClass | uppercase}}_REPOSITORY_TOKEN,
} from '../../domain/repositories/i-{{aggregateClassKebab}}.repository';

@Injectable()
export class {{aggregateClass}}Repository implements I{{aggregateClass}}Repository {
  constructor(
    @InjectRepository({{aggregateClass}})
    private readonly ormRepo: Repository<{{aggregateClass}}>,
  ) {}

{{#each methods}}
  async {{name}}({{#each parameters}}{{name}}: {{type}}{{#unless @last}}, {{/unless}}{{/each}}): {{returnType}} {
    {{#if (startsWith name "findById")}}
    return this.ormRepo.findOneBy({ id }) ?? null;
    {{else if (startsWith name "findAll")}}
    return this.ormRepo.find();
    {{else if (startsWith name "save")}}
    return this.ormRepo.save({{firstParam parameters}});
    {{else if (startsWith name "delete")}}
    await this.ormRepo.delete(id);
    {{else}}
    // TODO: implement {{name}}
    throw new Error('Not implemented');
    {{/if}}
  }

{{/each}}
}
