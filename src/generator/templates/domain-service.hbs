/**
 * {{className}} — Domain Service
 * Generated by ddd-codegen | Context: {{contextName}}
 * Metamodel: DCSL DOpt{type=Service}
 *
 * Domain Services encapsulate domain logic that doesn't naturally
 * fit within a single Entity or Value Object (Evans DDD §5).
 * Corresponds to AGL ANode executable nodes at domain layer.
 */

import { Injectable } from '@nestjs/common';

@Injectable()
export class {{className}} {
{{#each methods}}
  /**
   * AGL module action: {{moduleAction}}
   */
  {{#if isAsync}}async {{/if}}{{name}}(
    {{#each parameters}}{{name}}: {{type}}{{#unless @last}},
    {{/unless}}{{/each}}
  ): {{#if isAsync}}Promise<{{returnType}}>{{else}}{{returnType}}{{/if}} {
    // TODO: implement domain logic for {{name}}
    throw new Error('Not implemented');
  }

{{/each}}
}
