/**
 * OrderController — Presentation Layer
 * Generated by ddd-codegen | Context: PerfContext
 * Metamodel: MOSA Controller component (View + Controller in module class)
 *
 * Wraps use-case commands/queries behind HTTP endpoints.
 * RBAC guards protect each endpoint based on RBAC permissions.
 */

import {
  Controller, Get, Post, Put, Delete,
  Body, Param, HttpCode, HttpStatus, UseGuards,
} from '@nestjs/common';
import { CommandBus, QueryBus } from '@nestjs/cqrs';
import { RbacGuard } from '../../../auth/guards/rbac.guard';
import { RequirePermissions } from '../../../auth/decorators/permissions.decorator';
import { Permission } from '../../../auth/rbac/permissions.enum';

@Controller('perf-context/orders')
@UseGuards(RbacGuard)
export class OrderController {
  constructor(
    private readonly commandBus: CommandBus,
    private readonly queryBus: QueryBus,
  ) {}

  @Get()
  @RequirePermissions(Permission.VIEW_ORDERS)
  async findAll() {
    // TODO: dispatch FindAllOrderQuery
    return [];
  }

  @Get(':id')
  @RequirePermissions(Permission.VIEW_ORDERS)
  async findOne(@Param('id') id: string) {
    // TODO: dispatch FindOneOrderQuery
    return { id };
  }

  @Post()
  @HttpCode(HttpStatus.CREATED)
  @RequirePermissions(Permission.CREATE_ORDER)
  async create(@Body() dto: CreateOrderDto) {
    // TODO: dispatch CreateOrderCommand
    return dto;
  }

  @Put(':id')
  @RequirePermissions(Permission.UPDATE_ORDER)
  async update(@Param('id') id: string, @Body() dto: UpdateOrderDto) {
    // TODO: dispatch UpdateOrderCommand
    return { id, ...dto };
  }

  @Delete(':id')
  @HttpCode(HttpStatus.NO_CONTENT)
  @RequirePermissions(Permission.DELETE_ORDER)
  async remove(@Param('id') id: string) {
    // TODO: dispatch DeleteOrderCommand
  }
}

// ─── DTOs ────────────────────────────────────────────────────────────────────

export class CreateOrderDto {
  name: String;
  amount: Int;
  active: Boolean;
  createdAt: Date;
  updatedAt?: Date;

}

export class UpdateOrderDto {
  name?: String;
  amount?: Int;
  active?: Boolean;
  createdAt?: Date;
  updatedAt?: Date;

}
