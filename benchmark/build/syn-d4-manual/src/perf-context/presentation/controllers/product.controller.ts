/**
 * ProductController — Presentation Layer
 * Generated by ddd-codegen | Context: PerfContext
 * Metamodel: MOSA Controller component (View + Controller in module class)
 *
 * Wraps use-case commands/queries behind HTTP endpoints.
 * RBAC guards protect each endpoint based on RBAC permissions.
 */

import {
  Controller, Get, Post, Put, Delete,
  Body, Param, HttpCode, HttpStatus, UseGuards,
} from '@nestjs/common';
import { CommandBus, QueryBus } from '@nestjs/cqrs';
import { RbacGuard } from '../../../auth/guards/rbac.guard';
import { RequirePermissions } from '../../../auth/decorators/permissions.decorator';
import { Permission } from '../../../auth/rbac/permissions.enum';

@Controller('perf-context/products')
@UseGuards(RbacGuard)
export class ProductController {
  constructor(
    private readonly commandBus: CommandBus,
    private readonly queryBus: QueryBus,
  ) {}

  @Get()
  @RequirePermissions(Permission.VIEW_PRODUCTS)
  async findAll() {
    // TODO: dispatch FindAllProductQuery
    return [];
  }

  @Get(':id')
  @RequirePermissions(Permission.VIEW_PRODUCTS)
  async findOne(@Param('id') id: string) {
    // TODO: dispatch FindOneProductQuery
    return { id };
  }

  @Post()
  @HttpCode(HttpStatus.CREATED)
  @RequirePermissions(Permission.CREATE_PRODUCT)
  async create(@Body() dto: CreateProductDto) {
    // TODO: dispatch CreateProductCommand
    return dto;
  }

  @Put(':id')
  @RequirePermissions(Permission.UPDATE_PRODUCT)
  async update(@Param('id') id: string, @Body() dto: UpdateProductDto) {
    // TODO: dispatch UpdateProductCommand
    return { id, ...dto };
  }

  @Delete(':id')
  @HttpCode(HttpStatus.NO_CONTENT)
  @RequirePermissions(Permission.DELETE_PRODUCT)
  async remove(@Param('id') id: string) {
    // TODO: dispatch DeleteProductCommand
  }
}

// ─── DTOs ────────────────────────────────────────────────────────────────────

export class CreateProductDto {
  name: String;
  amount: Int;
  active: Boolean;
  createdAt: Date;
  updatedAt?: Date;

}

export class UpdateProductDto {
  name?: String;
  amount?: Int;
  active?: Boolean;
  createdAt?: Date;
  updatedAt?: Date;

}
