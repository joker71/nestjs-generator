/**
 * InvoiceRepository — Infrastructure Repository Implementation
 * Generated by ddd-codegen | Context: PerfContext
 * Implements: IInvoiceRepository
 * Layer: Infrastructure — adapts persistence to domain repository interface.
 *
 * Replace TypeORM with your preferred ORM / data-access layer.
 */

import { Injectable } from '@nestjs/common';
import { InjectRepository } from '@nestjs/typeorm';
import { Repository } from 'typeorm';
import { Invoice } from '../../domain/entities/invoice.entity';
import {
  IInvoiceRepository,
    INVOICE_REPOSITORY_TOKEN,
} from '../../domain/repositories/i-invoice.repository';

@Injectable()
export class InvoiceRepository implements IInvoiceRepository {
  constructor(
    @InjectRepository(Invoice)
    private readonly ormRepo: Repository<Invoice>,
  ) {}

  async findById(id: string): Promise<Invoice | null> {
    return this.ormRepo.findOneBy({ id }) ?? null;
  }

  async findAll(): Promise<Invoice[]> {
    return this.ormRepo.find();
  }

  async save(invoice: Invoice): Promise<Invoice> {
    return this.ormRepo.save(invoice);
  }

  async delete(id: string): Promise<void> {
    await this.ormRepo.delete(id);
  }

}
