/**
 * CreateInvoiceUseCase — Application Use Case
 * Generated by ddd-codegen | Context: PerfContext
 * Metamodel: AGL ANode (application-layer action sequence / SAA)
 *
 * Corresponds to AGL Structured Atomic Action (SAA):
 *   newObject → setDataFieldValues → createObject
 * Implements NestJS CQRS ICommandHandler pattern.
 */

import { CommandHandler, ICommandHandler, EventBus } from '@nestjs/cqrs';
import { Inject } from '@nestjs/common';
import { Invoice } from '../../domain/entities/invoice.entity';
import {
  IInvoiceRepository,
  INVOICE_REPOSITORY_TOKEN,
} from '../../domain/repositories/i-invoice.repository';

// ─── Command (AGL MAct input / fieldValSet) ──────────────────────────────────

export class CreateInvoiceCommand {
  constructor(
    public readonly name: String,
    public readonly amount: Int,
    public readonly active: Boolean,
  ) {}
}

// ─── Handler (AGL ANode action sequence executor) ────────────────────────────

@CommandHandler(CreateInvoiceCommand)
export class CreateInvoiceUseCase implements ICommandHandler<CreateInvoiceCommand> {
  constructor(
    @Inject(INVOICE_REPOSITORY_TOKEN)
    private readonly invoiceRepository: IInvoiceRepository,
    private readonly eventBus: EventBus,
  ) {}

  async execute(command: CreateInvoiceCommand): Promise<Invoice> {
    const { name, amount, active } = command;

    // AGL open → newObject: instantiate aggregate
    const invoice = new Invoice(
      name, amount, active
    );

    // AGL createObject: persist via repository
    const saved = await this.invoiceRepository.save(invoice);

    // AGL post-state: publish domain events
    // this.eventBus.publish(new InvoiceCreatedEvent(saved.id));

    return saved;
  }
}
