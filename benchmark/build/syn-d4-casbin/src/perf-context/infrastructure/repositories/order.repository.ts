/**
 * OrderRepository — Infrastructure Repository Implementation
 * Generated by ddd-codegen | Context: PerfContext
 * Implements: IOrderRepository
 * Layer: Infrastructure — adapts persistence to domain repository interface.
 *
 * Replace TypeORM with your preferred ORM / data-access layer.
 */

import { Injectable } from '@nestjs/common';
import { InjectRepository } from '@nestjs/typeorm';
import { Repository } from 'typeorm';
import { Order } from '../../domain/entities/order.entity';
import {
  IOrderRepository,
    ORDER_REPOSITORY_TOKEN,
} from '../../domain/repositories/i-order.repository';

@Injectable()
export class OrderRepository implements IOrderRepository {
  constructor(
    @InjectRepository(Order)
    private readonly ormRepo: Repository<Order>,
  ) {}

  async findById(id: string): Promise<Order | null> {
    return this.ormRepo.findOneBy({ id }) ?? null;
  }

  async findAll(): Promise<Order[]> {
    return this.ormRepo.find();
  }

  async save(order: Order): Promise<Order> {
    return this.ormRepo.save(order);
  }

  async delete(id: string): Promise<void> {
    await this.ormRepo.delete(id);
  }

}
