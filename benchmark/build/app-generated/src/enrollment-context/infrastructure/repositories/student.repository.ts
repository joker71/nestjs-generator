/**
 * StudentRepository — Infrastructure Repository Implementation
 * Generated by ddd-codegen | Context: EnrollmentContext
 * Implements: IStudentRepository
 * Layer: Infrastructure — adapts persistence to domain repository interface.
 *
 * Replace TypeORM with your preferred ORM / data-access layer.
 */

import { Injectable } from '@nestjs/common';
import { InjectRepository } from '@nestjs/typeorm';
import { Repository } from 'typeorm';
import { Student } from '../../domain/entities/student.entity';
import {
  IStudentRepository,
    STUDENT_REPOSITORY_TOKEN,
} from '../../domain/repositories/i-student.repository';

@Injectable()
export class StudentRepository implements IStudentRepository {
  constructor(
    @InjectRepository(Student)
    private readonly ormRepo: Repository<Student>,
  ) {}

  async findById(id: string): Promise<Student> {
    return this.ormRepo.findOneBy({ id }) ?? null;
  }

  async findByEmail(email: string): Promise<Student> {
    // TODO: implement findByEmail
    throw new Error('Not implemented');
      }

  async findAll(): Promise<Student[]> {
    return this.ormRepo.find();
  }

  async save(student: Student): Promise<Student> {
    return this.ormRepo.save(student);
  }

  async delete(id: string): Promise<void> {
    await this.ormRepo.delete(id);
  }

}
