/**
 * CourseModuleRepository — Infrastructure Repository Implementation
 * Generated by ddd-codegen | Context: EnrollmentContext
 * Implements: ICourseModuleRepository
 * Layer: Infrastructure — adapts persistence to domain repository interface.
 *
 * Replace TypeORM with your preferred ORM / data-access layer.
 */

import { Injectable } from '@nestjs/common';
import { InjectRepository } from '@nestjs/typeorm';
import { Repository } from 'typeorm';
import { CourseModule } from '../../domain/entities/course-module.entity';
import {
  ICourseModuleRepository,
    COURSE_MODULE_REPOSITORY_TOKEN,
} from '../../domain/repositories/i-course-module.repository';

@Injectable()
export class CourseModuleRepository implements ICourseModuleRepository {
  constructor(
    @InjectRepository(CourseModule)
    private readonly ormRepo: Repository<CourseModule>,
  ) {}

  async findById(id: string): Promise<CourseModule> {
    return this.ormRepo.findOneBy({ id }) ?? null;
  }

  async findBySemester(semester: number): Promise<CourseModule[]> {
    // TODO: implement findBySemester
    throw new Error('Not implemented');
      }

  async findAll(): Promise<CourseModule[]> {
    return this.ormRepo.find();
  }

  async save(courseModule: CourseModule): Promise<CourseModule> {
    return this.ormRepo.save(courseModule);
  }

  async delete(id: string): Promise<void> {
    await this.ormRepo.delete(id);
  }

}
