/**
 * StudentController — Presentation Layer
 * Generated by ddd-codegen | Context: EnrollmentContext
 * Metamodel: MOSA Controller component (View + Controller in module class)
 *
 * Wraps use-case commands/queries behind HTTP endpoints.
 * RBAC guards protect each endpoint based on RBAC permissions.
 */

import {
  Controller, Get, Post, Put, Delete,
  Body, Param, HttpCode, HttpStatus, UseGuards,
} from '@nestjs/common';
import { CommandBus, QueryBus } from '@nestjs/cqrs';
import { RbacGuard } from '../../../auth/guards/rbac.guard';
import { RequirePermissions } from '../../../auth/decorators/permissions.decorator';
import { Permission } from '../../../auth/rbac/permissions.enum';

@Controller('enrollment-context/students')
@UseGuards(RbacGuard)
export class StudentController {
  constructor(
    private readonly commandBus: CommandBus,
    private readonly queryBus: QueryBus,
  ) {}

  @Get()
  @RequirePermissions(Permission.VIEW_STUDENTS)
  async findAll() {
    // TODO: dispatch FindAllStudentQuery
    return [];
  }

  @Get(':id')
  @RequirePermissions(Permission.VIEW_STUDENTS)
  async findOne(@Param('id') id: string) {
    // TODO: dispatch FindOneStudentQuery
    return { id };
  }

  @Post()
  @HttpCode(HttpStatus.CREATED)
  @RequirePermissions(Permission.MANAGE_STUDENTS)
  async create(@Body() dto: CreateStudentDto) {
    // TODO: dispatch CreateStudentCommand
    return dto;
  }

  @Put(':id')
  @RequirePermissions(Permission.MANAGE_STUDENTS)
  async update(@Param('id') id: string, @Body() dto: UpdateStudentDto) {
    // TODO: dispatch UpdateStudentCommand
    return { id, ...dto };
  }

  @Delete(':id')
  @HttpCode(HttpStatus.NO_CONTENT)
  @RequirePermissions(Permission.DELETE_STUDENTS)
  async remove(@Param('id') id: string) {
    // TODO: dispatch DeleteStudentCommand
  }
}

// ─── DTOs ────────────────────────────────────────────────────────────────────

export class CreateStudentDto {
  name: string;
  email: string;
  helpRequested: boolean;
  createdAt: Date;
  updatedAt?: Date;

}

export class UpdateStudentDto {
  name?: string;
  email?: string;
  helpRequested?: boolean;
  createdAt?: Date;
  updatedAt?: Date;

}
