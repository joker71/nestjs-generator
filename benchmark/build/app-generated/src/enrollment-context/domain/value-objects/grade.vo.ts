/**
 * Grade — DDD Value Object
 * Generated by ddd-codegen | Context: EnrollmentContext
 * Metamodel: DCSL DClass{mutable=false} — immutable by design
 */

export class Grade {
  readonly internalMark: number;
  readonly examMark: number;

  private constructor(
    internalMark: number,
    examMark: number
  ) {
    this.internalMark = internalMark;
    this.examMark = examMark;
  }

  /**
   * Factory method — validates and constructs the Value Object.
   * Corresponds to DCSL DOpt{type=ObjectFormConstructor}
   */
  static create(
    internalMark: number,
    examMark: number
  ): Grade {
    // TODO: add validation / invariants (OCL-style constraints)
    if (internalMark === undefined || internalMark === null) throw new Error('Grade.internalMark is required');
    if (examMark === undefined || examMark === null) throw new Error('Grade.examMark is required');
    return new Grade(internalMark, examMark);
  }

  /**
   * Value equality — two Value Objects are equal when all their fields match.
   * Corresponds to DCSL DClass{mutable=false} identity semantics.
   */
  equals(other: Grade): boolean {
    if (!(other instanceof Grade)) return false;
    return this.internalMark === other.internalMark &&
      this.examMark === other.examMark;
  }

  public finalGrade(): string {
    // TODO: implement
    throw new Error('Not implemented');
  }

  public isPassing(): boolean {
    // TODO: implement
    throw new Error('Not implemented');
  }

}
