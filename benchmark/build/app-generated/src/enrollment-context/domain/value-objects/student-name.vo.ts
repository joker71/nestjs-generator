/**
 * StudentName — DDD Value Object
 * Generated by ddd-codegen | Context: EnrollmentContext
 * Metamodel: DCSL DClass{mutable=false} — immutable by design
 */

export class StudentName {
  readonly firstName: string;
  readonly lastName: string;

  private constructor(
    firstName: string,
    lastName: string
  ) {
    this.firstName = firstName;
    this.lastName = lastName;
  }

  /**
   * Factory method — validates and constructs the Value Object.
   * Corresponds to DCSL DOpt{type=ObjectFormConstructor}
   */
  static create(
    firstName: string,
    lastName: string
  ): StudentName {
    // TODO: add validation / invariants (OCL-style constraints)
    if (firstName === undefined || firstName === null) throw new Error('StudentName.firstName is required');
    if (lastName === undefined || lastName === null) throw new Error('StudentName.lastName is required');
    return new StudentName(firstName, lastName);
  }

  /**
   * Value equality — two Value Objects are equal when all their fields match.
   * Corresponds to DCSL DClass{mutable=false} identity semantics.
   */
  equals(other: StudentName): boolean {
    if (!(other instanceof StudentName)) return false;
    return this.firstName === other.firstName &&
      this.lastName === other.lastName;
  }

  public fullName(): string {
    // TODO: implement
    throw new Error('Not implemented');
  }

  public initials(): string {
    // TODO: implement
    throw new Error('Not implemented');
  }

}
