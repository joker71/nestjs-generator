/**
 * SClassRepository — Infrastructure Repository Implementation
 * Generated by ddd-codegen | Context: SClassContext
 * Implements: ISClassRepository
 * Layer: Infrastructure — adapts persistence to domain repository interface.
 *
 * Replace TypeORM with your preferred ORM / data-access layer.
 */

import { Injectable } from '@nestjs/common';
import { InjectRepository } from '@nestjs/typeorm';
import { Repository } from 'typeorm';
import { SClass } from '../../domain/entities/s-class.entity';
import {
  ISClassRepository,
    S_CLASS_REPOSITORY_TOKEN,
} from '../../domain/repositories/i-s-class.repository';

@Injectable()
export class SClassRepository implements ISClassRepository {
  constructor(
    @InjectRepository(SClass)
    private readonly ormRepo: Repository<SClass>,
  ) {}

  async findById(id: string): Promise<SClass | null> {
    return this.ormRepo.findOneBy({ id }) ?? null;
  }

  async findAll(): Promise<SClass[]> {
    return this.ormRepo.find();
  }

  async save(sClass: SClass): Promise<SClass> {
    return this.ormRepo.save(sClass);
  }

  async delete(id: string): Promise<void> {
    await this.ormRepo.delete(id);
  }

}
