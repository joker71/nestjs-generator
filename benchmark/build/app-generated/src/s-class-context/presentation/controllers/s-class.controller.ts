/**
 * SClassController — Presentation Layer
 * Generated by ddd-codegen | Context: SClassContext
 * Metamodel: MOSA Controller component (View + Controller in module class)
 *
 * Wraps use-case commands/queries behind HTTP endpoints.
 * RBAC guards protect each endpoint based on RBAC permissions.
 */

import {
  Controller, Get, Post, Put, Delete,
  Body, Param, HttpCode, HttpStatus, UseGuards,
} from '@nestjs/common';
import { CommandBus, QueryBus } from '@nestjs/cqrs';
import { RbacGuard } from '../../../auth/guards/rbac.guard';
import { RequirePermissions } from '../../../auth/decorators/permissions.decorator';
import { Permission } from '../../../auth/rbac/permissions.enum';

@Controller('s-class-context/s-classs')
@UseGuards(RbacGuard)
export class SClassController {
  constructor(
    private readonly commandBus: CommandBus,
    private readonly queryBus: QueryBus,
  ) {}

  @Get()
  @RequirePermissions(Permission.VIEW_S_CLASSES)
  async findAll() {
    // TODO: dispatch FindAllSClassQuery
    return [];
  }

  @Get(':id')
  @RequirePermissions(Permission.VIEW_S_CLASSES)
  async findOne(@Param('id') id: string) {
    // TODO: dispatch FindOneSClassQuery
    return { id };
  }

  @Post()
  @HttpCode(HttpStatus.CREATED)
  @RequirePermissions(Permission.CREATE_S_CLASS)
  async create(@Body() dto: CreateSClassDto) {
    // TODO: dispatch CreateSClassCommand
    return dto;
  }

  @Put(':id')
  @RequirePermissions(Permission.UPDATE_S_CLASS)
  async update(@Param('id') id: string, @Body() dto: UpdateSClassDto) {
    // TODO: dispatch UpdateSClassCommand
    return { id, ...dto };
  }

  @Delete(':id')
  @HttpCode(HttpStatus.NO_CONTENT)
  @RequirePermissions(Permission.DELETE_S_CLASS)
  async remove(@Param('id') id: string) {
    // TODO: dispatch DeleteSClassCommand
  }
}

// ─── DTOs ────────────────────────────────────────────────────────────────────

export class CreateSClassDto {
  name: string;
  capacity: int;
  academicYear: string;
  createdAt: Date;
  updatedAt?: Date;

}

export class UpdateSClassDto {
  name?: string;
  capacity?: int;
  academicYear?: string;
  createdAt?: Date;
  updatedAt?: Date;

}
