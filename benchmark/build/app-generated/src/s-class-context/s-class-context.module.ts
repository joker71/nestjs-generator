/**
 * SClassContextModule — NestJS Bounded Context Module
 * Generated by ddd-codegen
 * Metamodel: MOSA Module class → NestJS @Module
 *
 * Each Bounded Context maps to one NestJS module (microservice boundary).
 * Follows the Layered Microservices Architecture paper:
 *   Presentation → Application → Domain → Infrastructure
 */

import { Module } from '@nestjs/common';
import { CqrsModule } from '@nestjs/cqrs';
import { SClassController } from './presentation/controllers/s-class.controller';
import { SClassRepository } from './infrastructure/repositories/s-class.repository';
import { S_CLASS_REPOSITORY_TOKEN } from './domain/repositories/i-s-class.repository';


import { CreateSClassUseCase } from './application/use-cases/create-s-class/create-s-class.use-case';

// ─── Command handlers (AGL ANode action sequences) ───────────────────────────
const CommandHandlers = [
  CreateSClassUseCase,
];

// ─── Domain services ─────────────────────────────────────────────────────────
const DomainServices = [
];

// ─── Repository providers (Infrastructure → Domain interface binding) ─────────
const RepositoryProviders = [
  {
    provide: S_CLASS_REPOSITORY_TOKEN,
    useClass: SClassRepository,
  },
];

@Module({
  imports: [CqrsModule],
  controllers: [
    SClassController,
  ],
  providers: [
    ...CommandHandlers,
    ...DomainServices,
    ...RepositoryProviders,
    SClassRepository,
  ],
  exports: [...RepositoryProviders, ...DomainServices],
})
export class SClassContextModule {}
