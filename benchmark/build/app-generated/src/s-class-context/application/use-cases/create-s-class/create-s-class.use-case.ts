/**
 * CreateSClassUseCase — Application Use Case
 * Generated by ddd-codegen | Context: SClassContext
 * Metamodel: AGL ANode (application-layer action sequence / SAA)
 *
 * Corresponds to AGL Structured Atomic Action (SAA):
 *   newObject → setDataFieldValues → createObject
 * Implements NestJS CQRS ICommandHandler pattern.
 */

import { CommandHandler, ICommandHandler, EventBus } from '@nestjs/cqrs';
import { Inject } from '@nestjs/common';
import { SClass } from '../../domain/entities/s-class.entity';
import {
  ISClassRepository,
  S_CLASS_REPOSITORY_TOKEN,
} from '../../domain/repositories/i-s-class.repository';

// ─── Command (AGL MAct input / fieldValSet) ──────────────────────────────────

export class CreateSClassCommand {
  constructor(
    public readonly name: string,
    public readonly capacity: int,
    public readonly academicYear: string,
  ) {}
}

// ─── Handler (AGL ANode action sequence executor) ────────────────────────────

@CommandHandler(CreateSClassCommand)
export class CreateSClassUseCase implements ICommandHandler<CreateSClassCommand> {
  constructor(
    @Inject(S_CLASS_REPOSITORY_TOKEN)
    private readonly sClassRepository: ISClassRepository,
    private readonly eventBus: EventBus,
  ) {}

  async execute(command: CreateSClassCommand): Promise<SClass> {
    const { name, capacity, academicYear } = command;

    // AGL open → newObject: instantiate aggregate
    const sClass = new SClass(
      name, capacity, academicYear
    );

    // AGL createObject: persist via repository
    const saved = await this.sClassRepository.save(sClass);

    // AGL post-state: publish domain events
    // this.eventBus.publish(new SClassCreatedEvent(saved.id));

    return saved;
  }
}
