/**
 * Role — RBAC Roles Enum
 * Generated by ddd-codegen
 * Metamodel: RBAC₁ Role Hierarchy (Sandhu et al. 1996)
 *
 * RBAC₁ extends RBAC₀ with role hierarchy.
 * A user assigned to a senior role inherits all permissions
 * of its junior roles.
 */

export enum Role {
  ADMIN = 'Admin',
  ENROLLMENT_OFFICER = 'EnrollmentOfficer',
  STUDENT_ROLE = 'StudentRole',
}

/**
 * Role hierarchy map (RBAC₁ — Sandhu96 §3).
 * Each role inherits the permissions of its parent.
 */
export const ROLE_HIERARCHY: Partial<Record<Role, Role>> = {
  [Role.ADMIN]: Role.ENROLLMENT_OFFICER,

};
